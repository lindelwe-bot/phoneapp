specVersion: "v1"
serviceId: "techconnect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "techconnect-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
